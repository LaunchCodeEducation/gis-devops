---
title: "JavaScript: Fundamentals Quiz"
date: 2021-03-01T12:38:36-06:00
summary: ""
categories: ["quizzes", "javascript"]
tags: ["fundamentals"]
---

> A variable declared using `let` can be reassigned.

<!-- 1. true -->

1. true
2. false

> A variable declared using `const` can be reassigned.

<!-- 2, false -->

1. true
2. false

> A mutable data type (like an array) **can not be mutated** if it has been declared as a `const`.

```js
const arr = [];
arr.push(1);
```

<!-- 1. false -->

1. true
2. false

> The *JavaScript interpreter* will assign which data type to an unassigned variable?

```js
let unassignedVariable;
```

<!-- 3. undefined -->

1. `null`
2. `false`
3. `undefined`
4. `0`
5. `NaN`

> Select the definition for type coercion:

<!-- 2. implicit conversion between data types -->

1. manual conversion between data types
2. implicit conversion between data types
3. converting a string into a number
4. converting a number into a string

> Select the two conditions when the JavaScript interpreter may perform type coercion:

<!-- 
- when performing a strict equality comparison
- when resolving a value to it's boolean representation (truthy/falsy)
-->

- when one of the many parse methods is encountered
- when performing a strict equality comparison
- when adding two numbers together
- when resolving a value to it's boolean representation (truthy/falsy)

> What is the difference between loose and strict equality?

<!-- 3. strict equality compare data types first and then values second -->

1. loose equality compares data types first and then values second
2. loose equality performs type coercion
3. strict equality compare data types first and then values second

> Select the appropriate statement about JavaScript:

<!-- 2. JavaScript is a dynamically typed interpreted programming language -->

1. JavaScript is a dynamically typed compiled programming language
1. JavaScript is a dynamically typed interpreted programming language
1. JavaScript is a strictly typed interpreted programming langauge
1. JavaScript is a strictly typed compiled programming language

> In JavaScript can all values can be resolved to a boolean representation?

<!-- true -->

- true
- false

> In the following list of values select all that are considered **falsy**:

<!-- 
- false
- 0
- ""
- null
- undefined
- NaN
 -->

- `true`
- `false`
- `1`
- `0`
- `""`
- `"Hello"`
- `null`
- `(a, b) => a+b`
- `undefined`
- `15.5`
- `NaN`

> What are the three ways to define a function?

<!--
- function expression
- function declaration
- arrow function
-->

- function expression
- abstract function
- functional paradigm
- function declaration
- arrow function
- anonymous function
