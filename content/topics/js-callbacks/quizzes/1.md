---
title: "JavaScript: Callbacks Quiz"
date: 2021-03-02T17:48:54Z
summary: ""
categories: ["quizzes", "javascript"]
tags: ["callbacks"]
---

<!-- you do not need to 1:1 LOs to quizzes -->

<!-- 
Terminology:

- (general) match definition to term
- (lang/tool) match purpose to syntax
- true/false
-->

<!-- 
 Concepts:

- given scenario label the terms 
- given scenario select concept
- multiple selection (checkboxes)
- true/false

-->

<!-- 
Capabilities:

- given scenario label the terms 
- given scenario select syntax

-->

## Theory

> A Higher Order Function satisfies which of the following conditions?

<!-- 
- receives another function as an argument
- returns a function as its output
-->

- receives another function as an argument
- contains recursive internal logic
- returns a function as its output
- receives anything except a function as an argument

> What feature of JavaScript enables the use of HOF?

<!-- 2. functions are first class citizens -->

1. JavaScript does not support HOF
2. functions are first class citizens
3. anonymous functions
4. optional function arguments

## Usage

> What is responsible for invoking the `callback`?:

<!-- 2. the `procedure` function invokes it -->

1. you invoke it **before** calling the `procedure`
2. the `procedure` function invokes it
3. you invoke it **after** calling the `procedure`

> Information is **sent out** of a `procedure` through:

<!-- 1. the arguments given to the `callback` when invoked -->

1. the arguments given to the `callback` when invoked
2. the output returned from the `callback` when invoked

> Information is **sent into** a `procedure` through:

<!-- 1. the arguments given to the `callback` when invoked -->

1. the arguments given to the `callback` when invoked
2. the output returned from the `callback` when invoked

> Every `callback` must receive arguments from the `procedure`

<!-- 2. false -->

1. true
2. false

> Every `callback` must return an output to the `procedure`

<!-- 2. false -->

1. true
2. false
